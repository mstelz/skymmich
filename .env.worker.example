# Worker-Only Environment Configuration
# Use this when running the worker standalone (without UI/API server)
# Copy this file to .env.worker and fill in your actual values

# Database Configuration (required)
# Must point to the same database as the main application
DATABASE_URL=

# Immich Integration (required for worker)
IMMICH_URL=http://localhost:2283
IMMICH_API_KEY=your_immich_api_key_here
IMMICH_STORAGE_PATH=/var/lib/immich/upload

# Astrometry.net Integration (required for worker)
ASTROMETRY_API_KEY=your_astrometry_api_key_here
ASTROMETRY_CHECK_INTERVAL=30
ASTROMETRY_POLL_INTERVAL=5
PLATE_SOLVE_MAX_CONCURRENT=3
ASTROMETRY_AUTO_RESUBMIT=false

# Server Connection (for Socket.io updates)
SERVER_URL=http://localhost:5000

# IMPORTANT NOTES:
# 1. This file is only used when running the worker as a standalone process
# 2. If you're running the full application with UI, configure settings through the admin panel instead
# 3. The worker will automatically detect if it should use env vars or database configuration
# 4. For production deployments, use environment variables or secret management systems